@* This component now uses independent player windows instead of an overlay panel *@

@code {
    // Component kept for backward compatibility but no longer displays UI
    // All playback is handled through independent player windows
}
